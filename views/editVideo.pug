extends layouts/main

block content
    .form-container
        form(action=routes.editVideo, method="post")
            input(type="text", placeholder="Title", name="title")
            input(type="email", placeholder="Description", name="description")
            input(type="submit", value="Update Video")
        a.form-container_link(herf=routes.deleteVideo) Delete Video